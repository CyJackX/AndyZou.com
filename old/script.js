// JavaScript Document
 $(document).ready(function () {
    $('.menu-button').click(function () {
      $('.menu').toggle(); /* toggle visibility of the menu */
      return false; /* prevent the default action */
    });
  });